/*### 🔍 สรุปเนื้อหาจาก **CPE102_15_20250128.pdf** (Lecture 15: Pointers I)

---

#### **📌 Memory Address (ที่อยู่หน่วยความจำ)**
- `&` (address operator) ใช้ในการคืนค่าที่อยู่หน่วยความจำของตัวแปร
- ตัวแปรแต่ละตัวจะมีที่อยู่ในหน่วยความจำที่แตกต่างกัน เช่น
  ```
  int x = 82;
  &x = 0x22fe3c;
  ```
- อาร์เรย์ (`array`) แต่ละองค์ประกอบมีที่อยู่ในหน่วยความจำเรียงกัน

---

#### **📌 Pointer (ตัวชี้)**
- Pointer เป็นตัวแปรที่เก็บ "ที่อยู่หน่วยความจำ" แทนค่าของตัวแปรโดยตรง
- ประกาศ pointer โดยใช้ `*` เช่น
  ```cpp
  int *myPtr;  // myPtr เป็นตัวชี้ไปยังตัวแปรประเภท int
  ```
- Pointer สามารถใช้ `NULL` หรือ `0` เพื่อกำหนดให้ไม่ชี้ไปที่ใด
  ```cpp
  int *p = NULL;
  ```

---

#### **📌 Pointer Assignment (การกำหนดค่าให้ Pointer)**
- Pointer สามารถชี้ไปยังตัวแปรที่มีประเภทเดียวกันได้
  ```cpp
  int x = 69;
  int *myPtr = &x; // myPtr เก็บที่อยู่ของ x
  ```
- ไม่สามารถกำหนดค่าตัวเลขโดยตรงให้ Pointer ได้ เช่น
  ```cpp
  int *p = 0x69;  // ❌ ผิด! ต้องใช้ที่อยู่จริงของตัวแปร
  ```

---

#### **📌 Pointer to Pointer (ตัวชี้ไปยังตัวชี้)**
- Pointer สามารถเก็บที่อยู่ของ Pointer ตัวอื่นได้
  ```cpp
  int x = 98;
  int *p1 = &x;
  int **p2 = &p1; // p2 ชี้ไปยัง p1
  ```
- ใช้ `**p2` เพื่อเข้าถึงค่าที่ตัว Pointer ตัวแรกชี้อยู่

---

#### **📌 Dereferencing Operator (`*`)**
- ใช้ `*` เพื่อดึงค่าจากที่อยู่หน่วยความจำที่ Pointer ชี้อยู่
  ```cpp
  int x = 55;
  int *p = &x;
  cout << *p; // แสดงค่า x (55)
  ```
- `&` และ `*` เป็น operator ตรงข้ามกัน (`&*p` เท่ากับ `p`)

---

#### **📌 Reference Variable (ตัวแปรอ้างอิง)**
- ตัวแปรอ้างอิง (`Reference`) เป็นชื่ออื่นของตัวแปรเดิม
  ```cpp
  int x = 17;
  int &y = x;  // y เป็นอีกชื่อของ x
  y = 55;  // x ก็เปลี่ยนเป็น 55 เช่นกัน
  ```
- ต้องกำหนดค่าตั้งแต่แรกและไม่สามารถเปลี่ยนแปลงตัวแปรที่อ้างอิงได้ภายหลัง

---

#### **📌 การส่งค่าให้ฟังก์ชันแบบต่าง ๆ**
1. **Pass-by-Value** (ส่งค่าคัดลอก)
   ```cpp
   void func(int x) { x = 2 * x; }
   ```
   - ฟังก์ชันใช้ค่าคัดลอก ไม่กระทบค่าตัวแปรต้นฉบับ

2. **Pass-by-Reference (ใช้ `&`)** 
   ```cpp
   void func(int &x) { x = 2 * x; }
   ```
   - เปลี่ยนค่าตัวแปรต้นฉบับได้

3. **Pass-by-Pointer (ใช้ `*`)**
   ```cpp
   void func(int *x) { *x = 2 * (*x); }
   ```
   - ใช้ pointer เพื่อแก้ไขค่าตัวแปรที่ส่งเข้าไป

4. **Pass-by-Array**
   ```cpp
   void func(int x[]) { x[0] = 2 * x[0]; }
   ```
   - ฟังก์ชันจะเปลี่ยนค่าของอาร์เรย์โดยตรง

---

#### **📌 ตัวอย่างการใช้ Pointer กับ `std::sort`**
- ใช้ `std::sort` กับอาร์เรย์ผ่าน Pointer
  ```cpp
  int data[] = {7, 8, 2, 1, 2, 4, 6, 9, 0, 1}; 
  sort(&data[0], &data[10]); // เรียงลำดับอาร์เรย์
  ```
- ใช้ pointer เพื่อกำหนดช่วงของข้อมูลที่ต้องการเรียงลำดับ

---

### ✅ **สรุปหลักการสำคัญ**
- Pointer ใช้เก็บ "ที่อยู่หน่วยความจำ" ของตัวแปร
- Dereferencing (`*`) ใช้เข้าถึงค่าที่ Pointer ชี้ไป
- Reference เป็นอีกชื่อหนึ่งของตัวแปรเดิม
- ฟังก์ชันสามารถรับค่าแบบ Pass-by-Value, Reference หรือ Pointer ได้
- Pointer ใช้ร่วมกับ `std::sort` ในการเรียงลำดับข้อมูล

---

📌 **เนื้อหาสำคัญของไฟล์นี้เน้นไปที่ Pointers, Dereferencing, Reference Variables และการใช้งานฟังก์ชันร่วมกับ Pointers ซึ่งเป็นพื้นฐานสำคัญของการจัดการหน่วยความจำใน C++***/