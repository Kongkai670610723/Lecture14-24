/*### สรุปเนื้อหา **Lecture 14: Sorting & Searching** จาก **CPE102_14_20250124.pdf**

---

### **1. Sorting (การเรียงลำดับ)**
การเรียงลำดับคือกระบวนการนำข้อมูลที่ไม่เป็นระเบียบมาเรียงให้เป็นลำดับที่ถูกต้อง เช่น จากค่าต่ำไปสูง หรือสูงไปต่ำ

📌 **อัลกอริทึมที่ใช้สำหรับการเรียงลำดับ**
- **Bubble Sort**
- **Insertion Sort**
- **Selection Sort**
- **Merge Sort**
- **Quicksort**

---

### **2. Bubble Sort**
📌 หลักการทำงาน:
1. เทียบค่าของสองตัวติดกัน หากตัวซ้ายมากกว่าตัวขวา ให้สลับที่กัน
2. ทำซ้ำกระบวนการนี้จากซ้ายไปขวา จนค่าที่มากสุด "ลอยขึ้นไป" ด้านขวาสุด
3. ทำซ้ำขั้นตอนนี้ **N-1 ครั้ง** (N คือจำนวนข้อมูลทั้งหมด)

✅ **จุดเด่น:** ง่ายต่อการเข้าใจและใช้งาน  
⚠️ **จุดด้อย:** ช้าสำหรับข้อมูลจำนวนมาก **(O(N²))**  
💡 **เพิ่มประสิทธิภาพ:** ใช้ตัวแปร boolean flag เพื่อตรวจสอบว่ามีการสลับหรือไม่ ถ้าไม่มีสลับเลย แสดงว่าข้อมูลถูกจัดเรียงแล้ว สามารถหยุดทำงานก่อนครบ N-1 รอบ

---

### **3. Insertion Sort**
📌 หลักการทำงาน:
1. เริ่มจากพิจารณาข้อมูลทีละตัวจากซ้ายไปขวา
2. นำข้อมูลที่เลือกไปแทรกในตำแหน่งที่เหมาะสมของข้อมูลที่เรียงไว้แล้ว
3. ทำซ้ำจนครบทุกตัว

✅ **จุดเด่น:** เร็วกว่าสำหรับชุดข้อมูลที่เรียงเกือบถูกแล้ว  
⚠️ **จุดด้อย:** ช้าสำหรับชุดข้อมูลขนาดใหญ่ **(O(N²))**

---

### **4. Selection Sort**
📌 หลักการทำงาน:
1. หา **ค่าที่เล็กที่สุด** ในช่วงที่ยังไม่ได้จัดเรียง
2. สลับตำแหน่งค่านั้นกับตัวแรกของช่วงที่ยังไม่ได้จัดเรียง
3. ทำซ้ำขั้นตอนนี้ **N-1 ครั้ง**

✅ **จุดเด่น:** ไม่ต้องสลับค่าบ่อย  
⚠️ **จุดด้อย:** ใช้เวลามาก **(O(N²))** แม้จะมีการเรียงบางส่วนแล้วก็ตาม

---

### **5. Searching (การค้นหา)**
📌 การค้นหาคือการหาค่าหรือข้อมูลที่ต้องการในชุดข้อมูล

📌 **วิธีการค้นหาที่สำคัญ**
1. **Linear Search** (การค้นหาเชิงเส้น)
   - ตรวจสอบค่าทีละตัวจากต้นจนจบ
   - **ใช้ได้กับอาร์เรย์ที่ยังไม่ได้เรียงลำดับ**
   - **ประสิทธิภาพ:**  
     - Best case: **O(1)** (เจอค่าที่ต้องการทันที)  
     - Worst case: **O(N)** (ต้องตรวจสอบทุกตัว)

2. **Binary Search** (การค้นหาแบบทวิภาค)
   - ใช้กับอาร์เรย์ที่ **เรียงลำดับแล้วเท่านั้น**
   - เปรียบเทียบค่ากลางของอาร์เรย์กับค่าที่ต้องการ
   - ถ้าค่าที่ต้องการ **น้อยกว่า** ค่ากลาง → ค้นหาต่อทางซ้าย
   - ถ้าค่าที่ต้องการ **มากกว่า** ค่ากลาง → ค้นหาต่อทางขวา
   - **ประสิทธิภาพ:** **O(log N)** ซึ่งเร็วกว่าการค้นหาเชิงเส้นมาก

📌 **เทคนิคการค้นหาเพิ่มเติม**
- **Self-Organizing Linear Search**: นำค่าที่ถูกค้นหาบ่อยมาไว้ด้านหน้าเพื่อลดเวลาในอนาคต
- **Depth-First Search (DFS)**: ใช้สำหรับโครงสร้างข้อมูลแบบต้นไม้ (Tree)
- **Breadth-First Search (BFS)**: ใช้สำหรับการค้นหาแบบกว้างในโครงสร้างต้นไม้ (Tree)

---

### **6. ตัวอย่าง**
📌 **ตัวอย่างการใช้ Linear Search**
```
Input name to find weight: Hashida
Hashida's weight is 72.5kg

Input name to find weight: Linlin
Linlin's weight is 99.99kg
```

📌 **ตัวอย่างการใช้ Binary Search**
```
Input a Student ID to search: 770610781
Student ID 770610781 got W in 261102
```

📌 **การใช้ Function Template**
- ทำให้โค้ดสามารถรองรับประเภทข้อมูลที่หลากหลายโดยไม่ต้องเขียนโค้ดใหม่สำหรับแต่ละประเภท

---

### **สรุป**
📌 **Sorting Algorithms**
- Bubble Sort, Insertion Sort, Selection Sort (O(N²))  
- Merge Sort, Quick Sort (O(N log N))

📌 **Searching Algorithms**
- Linear Search (O(N))  
- Binary Search (O(log N)) (ใช้ได้เฉพาะอาร์เรย์ที่เรียงแล้ว)

📌 **การเลือกใช้งาน**
- ถ้าข้อมูลมีขนาดเล็ก → **Bubble/Insertion/Selection Sort ก็เพียงพอ**
- ถ้าข้อมูลมีขนาดใหญ่ → **Merge Sort หรือ Quick Sort ดีกว่า**
- ถ้าค้นหาในอาร์เรย์ที่เรียงแล้ว → **ใช้ Binary Search**
- ถ้าค้นหาในอาร์เรย์ที่ไม่ได้เรียง → **ใช้ Linear Search**

---

เนื้อหาในไฟล์นี้เน้นไปที่ **Sorting และ Searching** ซึ่งเป็นพื้นฐานที่สำคัญของอัลกอริทึมการจัดการข้อมูล 🎯*/